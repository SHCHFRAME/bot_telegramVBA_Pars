{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Bot with Env",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/bot.py",
            "console": "integratedTerminal",
            "preLaunchTask": "Activate and Run Bot"
        }
    ]
}
